server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
     show-details: always
spring:
  cloud:
    gateway:
      default-filters:
       - RemoveRequestHeader=Cookie
       - PreserveHostHeader
      routes:
        - id: motorcycle-service
          uri: http://localhost:8585
          predicates:
            - Path=/api/v1/**
          filters:

        - id: cinema-service
          uri: http://localhost:8583
          predicates:
            - Path=/api/v1/**
          filters:




